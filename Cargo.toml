[package]
name = "burn-wav2vec2-multipa-repro"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "~0.21", features = ["flex", "cpu"] }
burn-store = "~0.21"

[build-dependencies]
burn-onnx = "~0.21"
